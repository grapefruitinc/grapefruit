<% page_title "Creating New Course" %>

<section>
  
  <div class="description">
    <h3>Create New Course</h3>
  </div>

  <div class="login">
    <%= form_for @course do |f| %>
      <% render "forms", f: f %>
    <% end %>
  </div>

</section>