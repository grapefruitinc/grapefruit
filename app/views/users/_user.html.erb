<p>
  <strong><%= user.name %></strong>
  <%= user.email %>
</p>
<p>
  <%= link_to "Remove From Course",
            course_course_user_path(@course, user),
            data: { confirm: "Are you sure you want to remove #{user.name} from #{@course.name}?" },
            method: :delete,
            class: "button alert small" %>
</p>
